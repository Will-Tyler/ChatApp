# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wire' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wire
	pod 'Firebase/Core'
	pod 'Firebase/Auth'
	pod 'GoogleSignIn'

  target 'WireTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WireUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
